version: "3.8"

services:
  pfula:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - ENV=production
    volumes:
      - pfula-data:/app/data
    restart: unless-stopped

volumes:
  pfula-data:
